import React from "react";
import styles from './contact.scss';

export default () => (
  <div className={styles.contact} id="contact">
    <header>
      <h1>Contact</h1>        
    </header>
  </div>
);
